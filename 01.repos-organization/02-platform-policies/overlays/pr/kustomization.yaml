apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

patches:
  - path: patches/env-patch.yaml
    target:
      kind: Placement
      name: placement-multicloud
